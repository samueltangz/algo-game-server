/**
 * create a new game (shuffle)
 * + player
 * - player
 * deal a card
 * 
 */

function marshal (dbGame) {
  return {
    id: dbUser['id'],
    name: dbUser['name'],
    rating: dbUser['rating']
  }
}

module.exports = {}